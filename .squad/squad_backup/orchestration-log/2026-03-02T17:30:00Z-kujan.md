# Kujan — Orchestration Log
**Timestamp:** 2026-03-02T17:30:00Z  
**Wave:** 1 + 2

## Wave 1 — CI/GHCR Publish
- **#85 CI workflow:** Implemented `.github/workflows/ci.yml` with test + lint jobs, Node 20, parallel execution on PR to main/dev
- **#86 Publish workflow:** Implemented `.github/workflows/publish.yml` with GHCR auth, dual-image build (arachne-gateway, arachne-portal), `:latest` + `:sha` tags

## Wave 2 — Terraform + Deploy
- **#87 Terraform Azure infra:** Provisioned Container Apps for gateway + portal, env var config (SIGNUPS_ENABLED=false for beta), separate resource groups and managed identities
- **#88 Deploy workflow:** Implemented `workflow_run` trigger on publish workflow completion, `az containerapp update` with image override and env vars

## Key Decisions
- CI triggers on PR; publish on push-to-main only
- Deploy waits for publish completion (workflow_run) to ensure images exist
- Database not in Terraform v1 (manual provisioning, terraform import in v2)
- SIGNUPS_ENABLED hardcoded in Terraform for clarity (invite-only beta)
- Gateway: 0.5 CPU / 1 GiB; Portal: 0.25 CPU / 0.5 GiB

## Status
**COMPLETE** — All 2 issues closed. Infra ready for beta launch.
