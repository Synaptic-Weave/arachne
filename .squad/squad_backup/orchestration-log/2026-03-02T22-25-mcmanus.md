# McManus Portal Docker Build Fix — 2026-03-02T22:25Z

## Task Completed
- **Issue:** portal Docker build failing during `tsc && vite build` step
- **Root Cause:** `portal/tsconfig.json` compiling test files that reference `global` (Node.js global unavailable in browser DOM lib)
- **Fix:** Added `exclude` pattern to `portal/tsconfig.json` to exclude test files from production TS compilation

## Files Modified
- `portal/tsconfig.json` — added `"exclude": ["src/**/__tests__/**", "src/**/*.test.ts", "src/**/*.test.tsx"]`

## Impact
- Docker build now passes
- All 635 existing tests continue to pass via Vitest
- No changes to test setup, test files, or Vite config

## Status
✅ Portal Docker build fixed. Ready for deployment.
